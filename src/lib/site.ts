/** Public product name (header, metadata, legal copy, emails). */
export const SITE_NAME = "Evermissed";

/** Canonical hostname without protocol. */
export const SITE_DOMAIN = "evermissed.com";

/** Default public site URL when `NEXT_PUBLIC_APP_URL` is unset. */
export const SITE_URL_PUBLIC = `https://${SITE_DOMAIN}`;
